// You can add JavaScript for interactivity (e.g., form validation, animations, etc.)
document.querySelector("form").addEventListener("submit", function (event) {
  event.preventDefault();
  alert("Message sent!");
});
